# 확장 설계 문서 — React/FastAPI 지도형 서비스

> 이 문서는 제출 ZIP에 포함하지 않는 확장 구현 기록이다.
> 최종 제출 기준 문서는 `README.md`, `설계문서.md`, `광주_3반_박건우_지역병원길잡이.ipynb`이다.

## 1. 문서 위치

| 구분 | 상태 |
|---|---|
| 과제 제출 핵심 | LangChain 노트북 |
| 이 문서의 역할 | 노트북 이후 React/FastAPI 지도형 확장 설계 기록 |
| 제출 ZIP 포함 여부 | 제외 |
| 제외 이유 | 과제 핵심은 `입력 → 프롬프트 → LLM → 결과 출력`이므로, 프론트 확장 구현은 발표 보조 범위 밖 |

## 2. 현재 확장 구조

```text
langchain/
├── backend/
│   ├── chain.py
│   ├── geo.py
│   └── main.py
└── frontend/
    ├── src/main.tsx
    ├── src/router/index.tsx
    ├── src/pages/
    ├── src/components/
    ├── src/hooks/
    ├── src/api/
    ├── src/lib/
    └── src/types/
```

## 3. 확장 앱의 목표

- 어르신과 보호자가 모바일 화면에서 병원 정보를 더 쉽게 볼 수 있게 한다.
- 지도를 첫 화면의 중심에 두고, 병원 목록과 증상 입력은 하단 패널에서 전환한다.
- 사용 방법은 별도 설명 페이지가 아니라 React Joyride로 실제 화면 위에서 안내한다.
- 404 페이지, Browser Router, 라우터 파일 분리를 적용한다.
- 현재 위치 마커는 병원/응급실 마커보다 높은 우선순위로 표시한다.

## 4. 제출 노트북과의 관계

| 항목 | 제출 노트북 | 확장 앱 |
|---|---|---|
| 핵심 평가 대상 | O | X |
| LangChain 체인 | 직접 설명 및 실행 | backend 모듈로 감싸서 사용 |
| 병원 데이터 | 공공데이터포털 API 우선, CSV fallback | backend API 응답 사용 |
| UI | 노트북 + Gradio 보조 | React 지도형 화면 |
| 제출 ZIP | 포함 | 제외 |

확장 앱은 제출 노트북의 결과를 시각적으로 보여주는 방향으로 설계했지만, 과제 평가의 중심은 노트북에 저장된 실행 흐름과 LangChain 컴포넌트 설명이다.

## 5. API 설계

### `POST /api/triage`

증상과 위치 정보를 받아 LangChain 체인 결과를 구조화 JSON으로 반환한다.

```json
{
  "symptom": "가슴이 쥐어짜듯 아프고 식은땀이 나요",
  "lat": 35.016,
  "lng": 126.71
}
```

응답은 다음 정보를 포함한다.

```json
{
  "triage": {
    "department": "내과",
    "severity": "emergency",
    "severity_label": "즉시 응급실",
    "reason": "...",
    "caution": "본 안내는 의료 진단이 아닙니다."
  },
  "facilities": [],
  "resolved_region": {
    "sido": "전라남도",
    "sigungu": "나주시"
  }
}
```

### `GET /api/nearby`

현재 위치 기준 주변 의료기관을 지도와 목록에 표시하기 위한 API다.

```text
GET /api/nearby?lat=35.016&lng=126.71&radius_km=5&limit=5
```

응답에는 기관명, 주소, 전화번호, 좌표, 응급실 여부, 거리 정보를 포함한다.

## 6. 데이터 원칙

- 병원명, 주소, 전화번호는 LLM이 생성하지 않고 공공 API 또는 fallback CSV 원문에서 가져온다.
- 공공 API는 주변 모든 병원을 완전하게 보장하지 않는다.
- API 조회 범위, 페이지 수, 반경, 승인 상태, 호출량 제한에 영향을 받는다.
- 기관별 진료과가 API에서 충분히 제공되지 않으면 병원명 기반 보조 추론을 사용한다.
- 진료과가 확인되지 않은 기관은 `진료과 확인 필요`로 표시하고 방문 전 전화 확인 안내를 붙인다.

## 7. 프론트엔드 설계 요약

- 라우터는 `src/router/index.tsx`에서 `createBrowserRouter`로 분리한다.
- `src/main.tsx`는 `RouterProvider`만 렌더링한다.
- 경로는 `/`, `/triage`, `*`를 사용한다.
- 데스크톱에서도 모바일 폭 중심으로 보여준다.
- 지도는 고정하고 병원 목록, 증상 입력, 결과는 하단 패널 탭으로 전환한다.
- 하단 패널은 드래그, 탭, 키보드 화살표로 높이를 조절한다.
- React Joyride는 진입 시 자동 실행하고 헤더 도움말 버튼으로 재실행한다.

## 8. 검증 기준

| 항목 | 확인 방법 |
|---|---|
| 라우터 | `/`, `/triage`, 없는 경로 확인 |
| 지도 | 내 위치, 병원, 응급실 마커 표시 확인 |
| 하단 패널 | 드래그, 탭, 키보드 이동 확인 |
| 도움말 | 자동 실행과 도움말 버튼 재실행 확인 |
| API 연결 | 증상 입력 후 `/api/triage` 응답 확인 |
| 빌드 | `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run build` |

## 9. 현재 제외 범위

- 로그인과 관리자 화면
- 실시간 병상 정보
- 병원 데이터 자동 배치 수집
- PWA와 React Native
- 별도 테스트 인프라

위 항목은 삭제나 영구 포기를 뜻하지 않는다. 과제 제출 범위와 확장 MVP에서 제외한다는 의미다.
