# 설계 문서 v2 — React 전환 + 위치 기반 확장

> v1 이후 React 전환과 위치 기반 확장의 구현 설계를 정리한다.
> `설계문서.md`는 노트북(v1) 제출용으로 유지하고, 이 문서는 React 전환 기준으로 작성한다.

---

## 1. 확정 사항

| 항목 | 결정 | 이유 |
|---|---|---|
| API 응답 | 구조화 JSON (`triage` + `facilities` + `resolved_region`) | 텍스트 문자열이면 React에서 카드·뱃지 렌더링 불가 |
| 거리 정렬 | 현재 위치 기준 `GET /api/nearby`와 triage 결과에 `distance_km` 표시 | 좌표 CSV와 Haversine 계산을 추가해 지도·목록에서 가까운 순으로 확인 |
| 폴더 구조 | `backend/`(FastAPI+체인 모듈), `frontend/`(Vite+React) 형제 | tech_blog의 frontend/backend 분리 규칙과 일치 |
| 실시간 병상 | v3로 보류, 응답 스키마에 `beds` 확장 필드만 예약 | 활용신청·키 발급이 선행 조건이라 v2 범위에서 제외 |

---

## 2. 아키텍처

```
[브라우저]                                        [서버]
React (:5173) ──POST /api/triage──▶ FastAPI (:8000)
  ├─ navigator.geolocation → lat/lng    ├─ lat/lng 있음 → Kakao 역지오코딩 → sido/sigungu
  └─ 거부 시 sido/sigungu 수동 입력     ├─ 없음 → 입력된 sido/sigungu 사용
        ◀──JSON──                        └─ core_chain.invoke() → JSON 응답
                                               └─ triage_chain → RunnableBranch → retriever 검색
```

- 외부 호출: LLM API(기존), Kakao Local API(역지오코딩, v2 추가분)
- Kakao REST 키는 서버 환경변수에만 둔다 — 프론트에 노출하지 않는다.

---

## 3. 폴더 구조

```
langchain/
├── 광주_3반_박건우_지역병원길잡이.ipynb   (v1 제출본, 그대로 유지)
├── data/                                (CSV, backend가 참조)
├── backend/
│   ├── chain.py                         (노트북 체인 로직을 모듈로 추출)
│   ├── geo.py                           (Kakao 역지오코딩)
│   └── main.py                          (FastAPI 엔드포인트, CORS)
└── frontend/                            (Vite + React)
    └── src/
        ├── main.tsx                     (RouterProvider)
        ├── router/index.tsx             (createBrowserRouter)
        ├── pages/                       (HomePage, TriagePage, NotFoundPage)
        ├── components/                  (templates, organisms, molecules, ui)
        ├── hooks/                       (geolocation, nearby, triage, Kakao map)
        ├── api/                         (axios 인스턴스, 인터셉터, triage API)
        ├── lib/                         (Kakao SDK, env, geolocation)
        └── utils/, types/, constants/
```

---

## 4. API 명세

### `POST /api/triage`

**요청**

```json
{ "symptom": "며칠째 기침이 심하고 열이 살짝 나요",
  "lat": 35.016, "lng": 126.71 }
```

- `lat`/`lng` 또는 `sido`/`sigungu` 둘 중 한 조합이 필수.
- `lat`/`lng`가 있으면 서버에서 역지오코딩해 `sido`/`sigungu`를 결정한다(수동 입력보다 우선).

**응답 200**

```json
{
  "triage": {
    "department": "내과",
    "severity": "clinic",
    "severity_label": "동네 의원",
    "reason": "...",
    "caution": "본 안내는 의료 진단이 아닙니다."
  },
  "facilities": [
    { "name": "빛가람내과의원", "type": "의원",
      "sido": "전라남도", "sigungu": "나주시",
      "address": "전라남도 나주시 빛가람로 100",
      "phone": "061-000-1001",
      "is_24h": null }
  ],
  "resolved_region": { "sido": "전라남도", "sigungu": "나주시" },
  "notice": null
}
```

- `resolved_region`: 실제 검색에 사용된 지역 — 좌표가 어떤 행정구역으로 해석됐는지 사용자에게 보여주기 위함.
- `notice`: 폴백 안내 문구("인접 지역 검색을 권합니다" 등). 검색 0건일 때 채움.
- `severity_label`은 코드 상수 매핑(v1 원칙 유지 — LLM이 만들지 않음).
- v3 확장 예약: `facilities[]`에 `beds`, `distance_km` 필드 추가 예정.

**에러**

| 코드 | 상황 |
|---|---|
| 400 | symptom 없음 / 위치 정보 둘 다 없음 |
| 502 | 역지오코딩 실패, LLM 호출·파싱 실패 (메시지에 원인 구분) |

### `GET /api/nearby?lat&lng&radius_km=30&limit=10`

현재 위치 기준 반경 내 의료기관을 거리순으로 반환 — 당근식 초기 지도 표시용.
`facilities[]`에 `kind`(hospital/emergency)와 `distance_km`를 포함한다.
CSV에 `lat`/`lng` 컬럼을 추가해 구현했으며, triage 결과의 `distance_km` 표시에도 같은 좌표를 사용한다.

### "지금 열려있는지" 표시 — 결정: 하지 않음

- 의원·병원의 실시간 영업 여부는 데이터가 없다 — 샘플 CSV에 진료시간 필드가 없고, 공공 표준데이터에도 영업시간은 제공되지 않는다.
- 응급실만 `is_24h` 컬럼으로 "24시간"을 표시한다(마커 라벨·카드 배지).
- 실데이터로 전환 시 카카오 장소 검색 API의 업체 정보 매칭을 검토 — 단 샘플 병원명은 가상이라 현재는 매칭 불가. v3에서 재논의.

---

## 5. 체인 모듈 분리 (`backend/chain.py`)

노트북 코드를 그대로 옮기되, 출력 경계를 둘로 나눈다.

```python
core_chain = (                                   # JSON용 — 판단+검색까지
    RunnableLambda(normalize_input)
    | RunnablePassthrough.assign(triage=triage_chain)
    | RunnablePassthrough.assign(facilities=facility_branch)
)

full_chain = core_chain | RunnableLambda(format_output)   # 텍스트용 — v1과 동일 결과
```

- `full_chain`의 출력은 v1 노트북과 완전히 동일한 문자열 → 회귀 검증 기준으로 사용.
- API는 `core_chain` 결과를 받아 `Document → dict`로 변환해 응답한다.

**metadata 보강(소규모)**: 지금 `page_content`에만 있는 `address`/`phone`/`is_24h`를 `hospital_to_doc`/`er_to_doc`의 metadata에도 넣는다 — 시설 정보를 문자열 파싱 없이 JSON 필드로 반환하기 위함. `page_content` 생성 방식은 그대로 두어 임베딩 품질을 유지한다.

**서버 기동 시 초기화**: CSV 로드 → Document 변환 → FAISS 구축을 앱 startup에서 1회 수행(노트북 셀 4·5와 동일 로직).

---

## 6. 위치 처리 (`backend/geo.py`)

- Kakao Local `GET /v2/local/geo/coord2regioncode.json?x={lng}&y={lat}`
- 응답의 `region_1depth_name` → sido, `region_2depth_name` → sigungu (region_type="B" 법정동 기준 행 우선)
- httpx 사용(이미 설치됨). 타임아웃 3초, 실패 시 502로 변환.
- 역지오코딩 결과가 비정형("세종특별자치시"처럼 2depth가 없는 경우)이면 `sigungu=None`으로 두고 sido 기준 검색 — 기존 폴백 사다리가 처리.

### 구현 중 발견한 엣지 — 행정구역 개편

- **관찰**: 나주 좌표(35.016, 126.71) 요청 시 카카오가 `전남광주통합특별시`(2026.7 출범)를 반환. 샘플 CSV는 구 명칭(`전라남도`/`광주광역시`)을 쓰므로 sido 필터가 0건이 됨.
- **대응**: `SIDO_LEGACY` 매핑으로 통합특별시 → 구 시도 튜플을 만들고, `_match`의 sido 조건을 후보 집합 포함 여부로 변경. `resolved_region`은 실제 변환된 신 명칭을 그대로 보여준다.
- **확인**: 좌표로 emergency 요청 → 전라남도 응급실 2곳 + 광주광역시 응급실 1곳 반환.

---

## 7. 프론트엔드 설계 (`frontend/`)

- Vite + React + TypeScript.
- 라우터는 `src/router/index.tsx`로 분리하고 `createBrowserRouter`를 사용한다.
- 현재 경로:
  - `/`: 시작 화면
  - `/triage`: 지도 + 주변 병원 + 증상 입력 + 결과
  - `*`: 404 페이지, 처음 화면과 진료 안내로 복귀 가능
- 공통 레이아웃은 `components/templates/AppShell.tsx`에서 관리한다.
- 사용 방법은 별도 페이지가 아니라 `react-joyride`로 실제 화면 위에 안내한다.
  - 앱 진입 시 자동으로 `/triage`에서 실행
  - 헤더 `도움말` 버튼으로 재실행
- 어르신 사용성을 기준으로 모바일 우선 UI를 적용한다.
  - 데스크톱에서도 `max-width: 430px` 모바일 폭으로 표시
  - 지도는 화면 가운데 고정
  - 아래 패널은 `nearby`/`symptom`/`result` 탭으로 전환
  - 패널 손잡이는 드래그, 탭, 키보드 화살표로 높이 조절
  - 전체 페이지가 길게 늘어지는 구조 대신 패널 내부만 스크롤
- 지도 UI:
  - 내 위치 마커를 가장 높은 우선순위로 표시
  - 병원은 파란색, 응급실은 빨간색, 내 위치는 파란색 타깃 마커
  - 병원 카드 선택 시 지도 중심 이동과 정보창 열기
- 흐름:
  1. 마운트 시 `navigator.geolocation.getCurrentPosition` 요청
  2. 성공하면 `/api/nearby`로 주변 병원을 지도와 목록에 표시
  3. 증상 입력 시 `POST /api/triage` 호출
  4. 결과 탭에서 진료과, 심각도, 주의문구, 추천 병원 목록 표시
- 개발 시 `vite.config.ts`의 `server.proxy`로 `/api` → `localhost:8000` 프록시. CORS는 로컬 개발만 허용.

---

## 8. 검증 계획

| 검증 | 방법 | 기준 |
|---|---|---|
| 체인 회귀 | `full_chain.invoke()`를 시나리오 1~3으로 실행 | 노트북 저장 출력과 동일 문자열 |
| API 단독 | uvicorn 기동 후 curl로 sido/sigungu 직접 요청 | 노트북과 동일 판정 + JSON 구조 |
| 역지오코딩 | 나주 좌표(35.016, 126.71)로 요청 | resolved_region과 legacy 매핑 처리 확인 |
| 프론트 | `/`, `/triage`, 없는 경로 확인 | 홈, 지도 화면, 404 정상 렌더링 |
| 지도 | 위치 허용 상태에서 `/api/nearby` 확인 | 내 위치·병원·응급실 마커 표시, 카드 선택 시 포커스 이동 |
| 도움말 | 최초 진입과 도움말 버튼 클릭 | Joyride 단계 표시 |
| 패널 | 손잡이 드래그·탭·화살표 키 | 하단 패널 높이 조절 |
| 위치 거부 | 권한 차단 상태로 수동 입력 | 수동 경로로 triage 요청 가능 |

---

## 9. 구현 순서

1. `backend/chain.py` 분리 → `full_chain` 회귀 확인 (외부 의존 없음)
2. `backend/main.py` — sido/sigungu 직접 입력만 먼저 → curl 확인
3. `backend/geo.py` — Kakao 키 발급 후 역지오코딩 연결
4. `frontend/` Vite 생성 → 라우터, 홈, triage, 404 구성
5. geolocation + Kakao 지도 연결 → 좌표 경로 동작 확인
6. CSV 좌표 컬럼 + Haversine 거리 정렬
7. 어르신 사용성을 기준으로 모바일 중심 UI, Joyride, 드래그 패널 적용

각 단계는 이전 단계의 동작을 깨지 않는지 확인한 뒤 진행한다.
